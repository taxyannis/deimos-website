// Reuses the Open Graph card for the Twitter/X share image so forwarded
// links carry the same restrained navy institutional preview everywhere.
export { default, alt, size, contentType } from "./opengraph-image";
