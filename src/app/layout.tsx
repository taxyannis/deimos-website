import type { Metadata } from "next";
import { displaySerif, bodySerif } from "@/lib/fonts";
import { Nav } from "@/components/layout/Nav";
import { Footer } from "@/components/layout/Footer";
import { SITE_URL } from "@/content/site";
import "./globals.css";

const TITLE = "Deimos Group | Reducing Capital Risk Through Structure, Process, and Execution";
const DESCRIPTION =
  "Deimos Group advises owners, sponsors, investors, operators and public-sector counterparties, reducing capital risk through transaction structure, process discipline and execution control.";

// The share image is generated by app/opengraph-image.tsx (and reused for
// Twitter via app/twitter-image.tsx) — Next injects the og:image/twitter:image
// tags automatically, so no `images` field is set here. metadataBase makes
// those generated image URLs absolute. Card is summary_large_image now that a
// real 1200x630 card exists.
export const metadata: Metadata = {
  metadataBase: new URL(SITE_URL),
  title: TITLE,
  description: DESCRIPTION,
  openGraph: {
    title: TITLE,
    description: DESCRIPTION,
    siteName: "Deimos Group",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: TITLE,
    description: DESCRIPTION,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${displaySerif.variable} ${bodySerif.variable} h-full antialiased`}
    >
      <body className="flex min-h-full flex-col">
        <Nav />
        <main id="main-content" className="flex flex-1 flex-col">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
